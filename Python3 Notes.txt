
""" - Triple quotes for multiline string.  Can also be used for comments, as strings which are not assigned to a variable are created and discarded. (Not the same as a real comment, but similar)

# - Comment.  Single line only.  Python has no multiline comments

Dictionary access - aDict["key"]

None - null?

Pass - Do nothing statement

function/method parameters - positional or specified by name.  Optional, will default to null or none if not specified.  Can set a default value in declaration.  Method signatures can be ambiguous during inheritance.

self is normally specified as first parameter in declaration, but is not specified when calling.

print()
pprint.pprint() - Pretty print dictionaries.  Requires "import pprint"

Concatenate strings - "a"+"b"
Concatenate string and int - "a"+str(int)

